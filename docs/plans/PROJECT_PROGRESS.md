# 项目进度总览（PROJECT_PROGRESS）

> 用途：作为 Codex/Gemini 每次新会话的“唯一进度真相源”，快速恢复上下文，避免重复沟通。  
> 更新时间：2026-02-26

## 1. 项目基线
- 项目名称：项目管理系统（面向领导展示项目数据与工作量，后续对接外部系统）
- 后端：Spring Boot 3 + JDK 17
- 前端：Vue 3 + Vite + TypeScript
- 当前模式：单人开发 + AI 协作
- 当前分支：`feature/business-usable-v1`
- 当前工作目录：`.worktrees/business-usable-v1`

## 2. 范围与边界（已确认）
- 当前目标：先达成业务可用版本（核心流程跑通）
- 本阶段核心模块：项目管理、需求管理、任务管理、Bug 管理、登录与基础展示
- 需求与任务边界：
  - 需求 = 业务目标/价值
  - 任务 = 执行工作项
  - 关系：需求 1 -> N 任务；任务完成不等于需求关闭

## 3. 里程碑进度

### 3.1 已完成
- ✅ Task 1：项目模块业务可用增强（TDD）
  - 新增项目字段：`owner/startDate/endDate/status`
  - 新增接口：
    - `GET /api/pm/projects?status=ONGOING|ARCHIVED`
    - `PUT /api/pm/projects/{id}/archive`
    - `PUT /api/pm/projects/{id}/restore`
  - 创建接口兼容旧调用：仅传 `name` 仍可创建项目
  - 测试稳定性改造：列表断言从“固定数量”调整为“包含目标项目”

### 3.2 进行中
- 🚧 Task 2（待启动）：需求模块业务可用增强

### 3.3 待办（按优先级）
- [ ] Task 2：需求模块（状态机/列表/关联任务）
- [ ] Task 3：任务模块（负责人、工时、状态流转、看板视图支撑）
- [ ] Task 4：Bug 模块（生命周期、归属、统计口径）
- [ ] Task 5：前端页面联调与交互完善
- [ ] Task 6：跨模块汇总展示（领导视角）

## 4. 最近一次会话记录
- 日期：2026-02-26
- 目标：完成 Task 1（项目模块业务可用）
- 关键变更：
  - `project-manager-server/src/main/java/com/hm/pm/pm/PmStore.java`
  - `project-manager-server/src/main/java/com/hm/pm/pm/project/ProjectController.java`
  - `project-manager-server/src/test/java/com/hm/pm/pm/ProjectBusinessUsableApiTest.java`
- 验证命令：
  - `mvn -q -Dtest=ProjectBusinessUsableApiTest test` ✅
  - `mvn -q test` ✅
- 结论：Task 1 完成，可进入 Task 2

## 5. 运行与验证清单
- 后端单测（指定）：`mvn -q -Dtest=<TestClass> test`
- 后端全量：`mvn -q test`
- 前端测试：`npm run test`
- 前端启动：`npm run dev`

## 6. 风险与阻塞
- 当前无阻塞
- 注意：测试上下文可能共享内存数据，列表类断言优先使用“包含性断言”而非“固定数量断言”

## 7. 下次会话启动提示词（可直接复制）
请先读取并复述以下文件后再编码：
1. `docs/plans/PROJECT_PROGRESS.md`
2. `docs/plans/2026-02-26-business-usable-v1-design.md`
3. `docs/plans/2026-02-26-business-usable-v1-implementation.md`

然后输出：
1) 你理解的当前进度（3-5条）  
2) 你建议的下一步计划（<=5步）  
3) 你将先写哪个失败测试（TDD）

## 8. 会话结束更新模板（每次复制追加）
```md
### 会话记录（YYYY-MM-DD HH:mm）
- 目标：
- 决策：
- 完成：
  - 
- 变更文件：
  - 
- 验证命令与结果：
  - 
- 遗留问题：
  - 
- 下一步：
  - 
```

